version: "2"

services:

  web:
    build: .
    image: glzjin/buusec_2019_code_review_1
    restart: always
    ports:
      - "127.0.0.1:8302:80"
    environment:
      - FLAG=flag{glzjin_wants_a_girl_firend}
